#lang eopl
(require 2htdp/image)
(require 2htdp/universe)

(define (render t)
  (text (number->string t) 12 "Red")
  )

(big-bang 100
     (on-tick sub1)
     (to-draw render)
     (stop-when zero?)
 )