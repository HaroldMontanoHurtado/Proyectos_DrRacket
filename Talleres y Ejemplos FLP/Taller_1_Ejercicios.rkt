#lang eopl
#| (list-set lista n x) que retorna una lista similar a lista, excepto que el n-simo
elemento (indexado desde cero) es x. |#
(define index 0)
(define prueba '(0 1 2 3 4 5))

(define list-set
  (lambda (lista n x)
    (cond
      [(null? lista) '()]
      [else (let ()
              (cond
                []
               )
             )
       ]
     )
   )
 )